# LongCat (OpenAI-compatible)
OPENAI_BASE_URL=
OPENAI_API_KEY=
EVE_MODEL=

# Route auth (shared secret read server-side by the Next.js middleware
# and the eve channel; the browser never sees it)
AGENT_API_KEY=

